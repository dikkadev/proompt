IMMEDIATE TODOS FOR NEXT SESSION:

1. Fix migration path in main.go (needs absolute path or embed)
2. Add go-git dependency when implementing git layer
3. Create repository layer with basic CRUD for prompts
4. Test JSON marshaling for StringSlice/JSONMap types
5. Add FTS5 triggers for auto-sync with main tables
6. Implement variable parsing ({{var}} and {{var:default}})

TESTING STRATEGY:
- Unit tests for variable parsing logic
- Integration tests with in-memory SQLite
- Test JSON marshaling edge cases

POTENTIAL GOTCHAS TO REMEMBER:
- Migration path resolution
- FTS5 sync triggers
- Variable circular dependency detection
- Git repo atomic operations with database

ARCHITECTURE NOTES:
- Keep git operations separate from database operations initially
- Implement variable resolution as pure functions first
- Consider context.Context for cancellation in database operations